"""SUPERB Metrics calculator"""
import os
import yaml
import argparse

def metric_calculate_pipeline(file_path, upload_count, email):
    with open("configs.yaml") as f:
        configs = yaml.safe_load(f)
    pass
#